{
  "name": "@iris/sdk-ts",
  "version": "0.0.1",
  "private": true,
  "description": "TypeScript SDK for IRIS-workflow host-backend (used by web-console).",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
