name: iris_sdk
description: Dart SDK for IRIS-workflow host-backend (used by Flutter apps).
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  dio: ^5.7.0
  web_socket_channel: ^3.0.0
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.0
  json_serializable: ^6.8.0
  lints: ^4.0.0
  test: ^1.25.0
